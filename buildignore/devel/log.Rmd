# To DO



# Questions

- Is explained variance by setting to 0 justified, do we need adjustment?
 How can NA values be dropped when NAs present
 

# Description

```{r}
# closeAllConnections()
data(multidrug)
library(foreach)
set.seed(10)
system.time({
tune.spca_res <- tune.spca(X = multidrug$ABC.trans, ncomp = 2, nrepeat = 3, kfold = 3, grid.keepX = seq(5,15,5))
})['elapsed']
tune.spca_res
```

